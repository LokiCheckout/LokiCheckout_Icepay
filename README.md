# Yireo_LokiCheckoutIcepay

**This is an add-on package to the LokiCheckout. It is only a dummy to kickstart new modules easily.**

## Installation
Install this package via composer (assuming you have setup the `composer.yireo.com` repository correctly already):
```bash
composer require yireo/magento2-loki-checkout-icepay
```

Next, enable this module:
```bash
bin/magento module:enable Yireo_LokiCheckoutIcepay
```

